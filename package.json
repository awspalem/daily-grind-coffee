{
  "name": "the-daily-grind",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:storefront": "npm run dev --workspace=apps/storefront",
    "dev:admin": "npm run dev --workspace=apps/admin",
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspace=packages/shared-types && npm run build --workspace=packages/db && npm run build --workspace=apps/api && npm run build --workspace=apps/storefront && npm run build --workspace=apps/admin",
    "test": "npm run test --workspace=apps/api && npm run test --workspace=apps/storefront"
  },
  "devDependencies": {
    "tsx": "^4.23.12",
    "typescript": "^5.7.2",
    "wrangler": "^3.111.0"
  }
}
