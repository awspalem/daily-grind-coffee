/**
 * Referral feature UI. Owned by one agent — see docs/roadmap-gaps.md.
 * Build all DOM through the helpers in ./shared.ts; do not edit main.ts or index.html.
 */
export function initReferral(app: any): void {
  void app;
}
